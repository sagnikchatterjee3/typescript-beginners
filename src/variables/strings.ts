let url: string = "<a href=`https://github.com`>Github</a>";
let github: string = `My github is ${url}/sagnikchatterjee3`;

console.log(github);

// typescript supports all the properties of strings that javascript supports and more 

console.log(github.charAt(1));
console.log(url.length);
console.log(github.indexOf('s'));
console.log(url.lastIndexOf('i'));